<%= form_tag( search_hospitals_path, method: "get") do |f| %>
  <%= label_tag("State Abbreviation:") %>
  <%= text_field_tag :provider_state %>
  <%= label_tag("Procedure:") %>
<select name="drg_definition">
  <% @drg_list_array.uniq.each do |drg| %>
  <option><%= drg[0] %></option>
  <% end %>
</select>

  <%= submit_tag "Search", :name => nil%>
<% end %>
