<body>
<h2>Search Results</h2>
<h4>Sorted by Average Covered Charges</h4>
<h5>Average Covered Charges: Average price that hospital billed Medicare</h5>
<h5>Average Medicare Payments: Average price that Medicare paid</h5>

<ul>
  <% @results.each do |result| %>
  <li><%= result["provider_name"] %> ,
    <%= result["provider_city"] %> ,
    <%= result["provider_state"] %> 
    Average Covered Charges <%= number_to_currency(result["average_covered_charges"].to_i) %> 
    Average Medicare Payments <%= number_to_currency(result["average_medicare_payments"].to_i) %> 
    <%= button_to "Favorite", action: "create", controller: "favorites", user_id: session[:user_id], data: result %></li>
  <% end %>
</ul>
</body>